# Verify Scripts

当前目录建议放可复用的验证脚本。

## 建议用途

- intake happy path 验证
- intake follow-up 验证
- report happy path 验证
- retired `/ai/extract` 的 `404` 验证

## 当前说明

- 当前仓库还没有稳定沉淀下来的验证脚本
- 如需新增脚本，优先让脚本覆盖当前公开端点 `/ai/intake` 与 `/ai/report`
